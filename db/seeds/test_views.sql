-- db/seeds/test_views.sql
-- UI Studio view seeds — intentionally empty.
--
-- Views are created and managed through the UI Studio interface (View Designer).
-- Do not seed views via SQL; use the designer to create and configure views.
--
-- The Product List view for the product entity was created through UI Studio
-- and is stored in the database with a live UUID (not a deterministic seed ID).

BEGIN;
-- No views seeded here.
COMMIT;
