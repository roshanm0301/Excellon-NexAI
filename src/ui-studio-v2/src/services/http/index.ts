// Phase 4 §5 — HTTP service implementations barrel
export { apiFetch, ApiError } from "./client"
export { createMetadataService } from "./metadata"
export { createCompilerService } from "./compiler"
export { createPreviewService } from "./preview"
export { createRegistryService } from "./registry"
export { createPresenceService } from "./presence"
