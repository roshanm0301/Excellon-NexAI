const PrivateOutlet = () => {
	// const auth = useSelector((state: any) => state.auth.userData?.Authorization);

	// return !auth ? (
	// 	<>
	// 		<MiniDrawer />
	// 		<Box component="main" sx={{ flexGrow: 1, p: 2 }}>
	// 			<DrawerHeader />
	// 			<Outlet />
	// 		</Box>
	// 	</>
	// ) : (
	// 	<Navigate to="/login" />
	// );

	return <></>;
};

export default PrivateOutlet;
