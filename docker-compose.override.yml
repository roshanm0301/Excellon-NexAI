version: "3.9"
services:
  api:
    build:
      target: development
    volumes:
      - ./src/go:/app
    environment:
      LOG_LEVEL: debug
  studio:
    build:
      target: development
    volumes:
      - ./src/react:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host"]
    ports:
      - "4173:5173"
    environment:
      API_PROXY_TARGET: http://api:8080
      VITE_AUTH_MODE: local
