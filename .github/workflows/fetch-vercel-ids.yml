# One-time setup workflow — no longer needed.
# Kept as a placeholder. Delete this file if desired.
name: Fetch Vercel IDs (completed)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Setup complete. Vercel IDs have been added as GitHub secrets.'
