# Disabled — Vercel is now the live deployment target.
# Re-enable this if you need GitHub Pages as a fallback.
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'GitHub Pages deployment is disabled. Vercel handles all deployments.'
