# Disabled — pages.yml now deploys directly from the feature branch.
# GITHUB_TOKEN-based pushes do not cascade to trigger downstream workflows,
# which was causing the need for manual trigger commits.
# This file is kept as a placeholder to avoid git conflicts.
name: Sync claude branch to main (disabled)

on:
  workflow_dispatch:  # manual only — not triggered automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled. pages.yml deploys directly from the feature branch.'
