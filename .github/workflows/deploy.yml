# Disabled — Vercel and Railway secrets are not configured in this repository.
# This workflow was producing constant red failures on every push with no value.
# Re-enable and configure VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID,
# and RAILWAY_TOKEN secrets when Vercel/Railway deployments are needed.
name: Deploy to Vercel + Railway (disabled)

on:
  workflow_dispatch:  # manual only — not triggered automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Configure Vercel/Railway secrets to re-enable this workflow.'
